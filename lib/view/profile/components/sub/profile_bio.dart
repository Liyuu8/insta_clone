import 'package:flutter/material.dart';

class ProfileBio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO:
    return Container(
      child: Text('ProfileBio'),
    );
  }
}
